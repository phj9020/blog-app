import styled from 'styled-components';

const PostContainer = styled.div`
    flex: 9;
`

function Posts() {
    return (
        <PostContainer>
            posts
            posts
            posts
            posts
            posts
            posts
            posts
            posts
            posts
            posts
            posts
            posts
        </PostContainer>
    )
}

export default Posts
